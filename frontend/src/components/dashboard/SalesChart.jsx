import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export default function SalesChart({ data = [], loading = false }) {
  const total = data.reduce((sum, m) => sum + Number(m.sales || 0), 0);
  return <div className="rounded-2xl border border-blue-100 bg-white p-5 shadow-[0_12px_35px_rgba(37,99,235,0.08)]"><div className="mb-4 flex items-center justify-between"><div><p className="text-sm text-slate-500">Sales overview</p><h3 className="text-lg font-bold text-slate-900">Orders per month</h3></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-600">{loading ? "Loading..." : `${total} orders`}</span></div><div className="h-72">{loading ? <div className="flex h-full items-center justify-center text-sm text-slate-400">Loading...</div> : data.length === 0 ? <div className="flex h-full items-center justify-center text-sm text-slate-400">No orders yet</div> : <ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><defs><linearGradient id="salesFillBlue" x1="0" x2="0" y1="0" y2="1"><stop offset="5%" stopColor="#2563eb" stopOpacity={0.35} /><stop offset="95%" stopColor="#2563eb" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="#e5edfb" strokeDasharray="3 3" /><XAxis dataKey="month" stroke="#94a3b8" /><YAxis stroke="#94a3b8" allowDecimals={false} /><Tooltip contentStyle={{ background: "#fff", borderColor: "#dbeafe", borderRadius: 14 }} /><Area type="monotone" dataKey="sales" name="Orders" stroke="#2563eb" fill="url(#salesFillBlue)" strokeWidth={3} /></AreaChart></ResponsiveContainer>}</div></div>;
}
