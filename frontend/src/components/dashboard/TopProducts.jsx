import { motion } from "framer-motion";

export default function TopProducts({ products = [], loading = false }) {
  return <div className="rounded-2xl border border-blue-100 bg-white p-5 shadow-[0_12px_35px_rgba(37,99,235,0.08)]"><p className="text-sm text-slate-500">Top selling products</p><h3 className="mb-4 text-lg font-bold text-slate-900">Best performers</h3>{loading ? <p className="py-6 text-center text-sm text-slate-400">Loading...</p> : products.length === 0 ? <p className="py-6 text-center text-sm text-slate-400">No sales yet</p> : <div className="space-y-4">{products.map((product, index) => <motion.div key={product.name} initial={{ opacity: 0, x: 12 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: index * .08 }} className="rounded-xl bg-blue-50/60 p-3"><div className="mb-2 flex justify-between text-sm"><span className="font-semibold text-slate-800">{product.name}</span><span className="text-slate-500">{product.sold} sold</span></div><div className="h-2 rounded-full bg-blue-100"><div className="h-2 rounded-full bg-blue-600" style={{ width: `${product.progress || 0}%` }} /></div></motion.div>)}</div>}</div>;
}
