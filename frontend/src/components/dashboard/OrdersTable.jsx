import { useMemo, useState } from "react";
import { Eye } from "lucide-react";
import { motion } from "framer-motion";
const orders = [{ id: "#ORD-1042", customer: "Riya Sharma", product: "Ashwagandha Capsules", amount: "₹12,148", status: "Pending", date: "2026-04-03" }, { id: "#ORD-1043", customer: "Karan Patel", product: "Herbal Oil", amount: "₹6,840", status: "Processing", date: "2026-04-03" }, { id: "#ORD-1044", customer: "Nina Verma", product: "Weight Loss Mix", amount: "₹24,240", status: "Shipped", date: "2026-04-02" }, { id: "#ORD-1045", customer: "Atul Joshi", product: "Diabetes Care Pack", amount: "₹19,192", status: "Delivered", date: "2026-04-01" }];
const statusStyle = { Pending: "bg-amber-100 text-amber-700", Processing: "bg-orange-100 text-orange-700", Shipped: "bg-blue-100 text-blue-700", Delivered: "bg-emerald-100 text-emerald-700" };
export default function OrdersTable() { const [search, setSearch] = useState(""); const filtered = useMemo(() => orders.filter((order) => Object.values(order).some((value) => value.toLowerCase().includes(search.toLowerCase()))), [search]); return <div className="rounded-2xl border border-blue-100 bg-white p-5 shadow-[0_12px_35px_rgba(37,99,235,0.08)]"><div className="mb-4 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><p className="text-sm text-slate-500">Recent orders</p><h3 className="text-lg font-bold text-slate-900">Order management</h3></div><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search order..." className="rounded-xl border border-blue-100 bg-blue-50/40 px-3 py-2 text-sm outline-none focus:border-blue-500" /></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-blue-50 text-xs uppercase text-blue-700"><tr>{["Order ID", "Customer", "Product", "Amount", "Status", "Date", "Action"].map((head) => <th key={head} className="px-3 py-3 font-semibold">{head}</th>)}</tr></thead><tbody>{filtered.map((order, index) => <motion.tr initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: index * 0.05 }} key={order.id} className="border-t border-blue-50 text-slate-700"><td className="px-3 py-3 font-semibold text-blue-600">{order.id}</td><td className="px-3 py-3">{order.customer}</td><td className="px-3 py-3">{order.product}</td><td className="px-3 py-3 font-semibold">{order.amount}</td><td className="px-3 py-3"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusStyle[order.status]}`}>{order.status}</span></td><td className="px-3 py-3">{order.date}</td><td className="px-3 py-3"><button className="rounded-lg border border-blue-100 p-2 text-blue-600 hover:bg-blue-50"><Eye size={15} /></button></td></motion.tr>)}</tbody></table></div></div>; }
